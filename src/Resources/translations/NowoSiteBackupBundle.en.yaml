restore:
  page:
    title: 'Restoring site'
    message: 'Restoring this site from a verified backup. Please wait…'
    done: 'Restore finished. Reloading…'
panel:
  title: 'Site Backup'
setup:
  wizard:
    title: 'Set up this site'
    subtitle: 'We will create the database, apply schema, and prepare an admin account.'
    continue: 'Continue'
    retry: 'Retry'
    running: 'Running the next setup step…'
  bootstrap:
    prompt: 'Choose how to bootstrap this site'
    guided: 'Guided setup (create admin)'
    full_database: 'Load full database dump'
    sql_path: 'SQL dump path (relative to project)'
    sql_hint: 'Required for full dump. Defaults: var/site-backup/full-import.sql or last-restore-dump.sql. Migrations and idempotent loaders still run; admin form is skipped if users already exist.'
  tab:
    requirements: 'Requirements'
    bootstrap_mode: 'Bootstrap mode'
    database_url: 'Database URL'
    database_create: 'Create database'
    cache_clear: 'Clear cache'
    schema_update: 'Update schema'
    migrations: 'Migrations'
    sql_file: 'Import SQL'
    console: 'Run command'
    admin_user: 'Admin user'
    sample_data: 'Sample data'
    marker: 'Finalize'
    custom: 'Custom step'
  check:
    ok: 'OK'
    needs_input: 'Waiting for input'
    blocked: 'Blocked'
  admin:
    create: 'Create admin'
  sample:
    prompt: 'Load sample / demo data?'
    load: 'Yes, load sample data'
    skip: 'No, skip'
  database:
    save: 'Save DATABASE_URL'
    skip: 'Skip (use existing env)'
  reason:
    heading: 'Setup is required'
    db_connection: 'The database is not reachable with the current DATABASE_URL (missing, wrong, or the DB service is down).'
    db_connection_hint: 'If .env already has DATABASE_URL, skip this step when it is optional. The next step can create the schema (doctrine:database:create).'
    db_connection_form: 'Doctrine cannot open the schema yet (often the database name is missing). When this step is optional, Skip keeps the current DATABASE_URL from .env — then create the schema on the next step — or paste a corrected URL.'
    db_schema_empty: 'The database connects but has no tables yet — migrations / import will create the schema.'
    db_schema_hint: 'Continue the wizard to run migrations (or import a full dump). You do not need to change DATABASE_URL if the connection already works.'
    marker_required: 'A setup.required marker is present (cold start or post-restore).'
    marker_done_missing: 'setup.done is missing while require_done_marker is enabled.'
    incomplete: 'Wizard in progress: %detail%'
  token:
    title: 'Setup token required'
    body: 'Open %path% with the configured setup.setup_token.'
  done:
    title: 'Setup complete'
    body: 'The site is ready. You can sign in with the admin account you created.'
    home: 'Go to homepage'
