{% extends layout_template %}

{% block nowo_site_backup_title %}{{ brandName }} — Setup token required{% endblock %}

{% block nowo_site_backup_content %}
    <div class="card">
        <h1>{{ 'setup.token.title'|trans({}, 'NowoSiteBackupBundle') }}</h1>
        <p class="sub">{{ 'setup.token.body'|trans({ '%path%': pathPrefix ~ '?token=…' }, 'NowoSiteBackupBundle') }}</p>
    </div>
{% endblock %}
