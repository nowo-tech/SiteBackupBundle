<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>{{ brandName }} — Setup token required</title>
</head>
<body>
    <h1>Setup token required</h1>
    <p>Open <code>{{ pathPrefix }}?token=…</code> with the configured <code>setup.setup_token</code>.</p>
</body>
</html>
